# Disasm2
**A better version of Disassembly implemented in cpp with command stacking and some low level features**

>Note: The language is not Turing Complete yet.

### For code generation and running
```bash 
g++ -Wall -o test main.cpp tokenizer.cpp lexer.cpp executor.cpp errors.cpp declarators.cpp 
# Run the compiled binary
./test


